# ── curate-ipsum environment ──────────────────────────────────────────────────
# Copy to .env and adjust values for your setup.

# LLM backend (cloud | local | mock)
LLM_BACKEND=mock
LLM_MODEL=codellama:7b
# For cloud: set your API key
# LLM_API_KEY=sk-...

# Chroma vector DB
# Leave unset for embedded (in-process) mode.
# Set for Docker Chroma service (docker compose up -d).
# CHROMA_HOST=localhost
# CHROMA_PORT=8000

# Embedding model (default: all-MiniLM-L6-v2, loaded locally)
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# angr Docker backend
# ANGR_DOCKER_IMAGE=curate-ipsum-angr-runner

# Graph store backend (sqlite | kuzu)
GRAPH_BACKEND=sqlite

# Logging
LOG_LEVEL=INFO
