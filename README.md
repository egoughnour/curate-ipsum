# Curate Ipsum

## mcp.json

Once `mcp.json` is set and saved, you can get the mcp server available to codex thusly:

- open `mcp.json` in the vs code editor
- click start server
check the GH copilot chat for the tools configuration
